{% extends 'base.html.twig' %}

{% block title %}Hello AjoutController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'E:/Projets/projet-RX/projetBD/src/Controller/AjoutController.php'|file_link(0) }}">src/Controller/AjoutController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'E:/Projets/projet-RX/projetBD/templates/ajout/index.html.twig'|file_link(0) }}">templates/ajout/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}

/*************************/
{% block title %}Medicament index{% endblock %}

{% block body %}
    <h1>Medicament index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>CodeProd</th>
            <th>PrixProd</th>
            <th>NoticeProd</th>
            <th>Fabriquant</th>
            <th>EstDisponible</th>
            <th>Expire_at</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for medicament in medicaments %}
            <tr>
                <td>{{ medicament.id }}</td>
                <td>{{ medicament.nom }}</td>
                <td>{{ medicament.codeProd }}</td>
                <td>{{ medicament.prixProd }}</td>
                <td>{{ medicament.noticeProd }}</td>
                <td>{{ medicament.fabriquant }}</td>
                <td>{{ medicament.estDisponible ? 'Yes' : 'No' }}</td>
                <td>{{ medicament.expireAt ? medicament.expireAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('medicament_show', {'id': medicament.id}) }}">show</a>
                    <a href="{{ path('medicament_edit', {'id': medicament.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('medicament_new') }}">Create new</a>
{% endblock %}
