<form method="post" action="{{ path('parapharmacie_delete', {'id': parapharmacie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parapharmacie.id) }}"><br>
    <button class="btn btn-danger">Delete</button>
</form>
