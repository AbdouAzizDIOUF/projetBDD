<form method="post" action="{{ path('category_med_delete', {'id': category_med.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_med.id) }}"><br>
    <button class="btn btn-danger">Supprimer</button>
</form>
